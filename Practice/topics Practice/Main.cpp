#include"Functionalities.h"

int main(){
    ArrayType arrData;
    Container vectorData;

    CreateArray(arrData);
    CreateVector(vectorData);
    DataDisplayInArray(arrData);
    DataDisplayInVector(vectorData);

    return 0;
}